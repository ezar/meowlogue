# Decision records

Short records of decisions that deviate from, or resolve an ambiguity in, the
Meowlogue spec (spec section 11). One file per decision, numbered, never edited
after acceptance — supersede with a new record instead.
